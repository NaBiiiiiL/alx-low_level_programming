#include "main.h"

/**
 * factorial - returns th factorial of a number
 * @n: number to return the factorial from
 *
 * return: factorial of n
 */

int factorial(int n)
{
	if (n < )
		return (-1);
	if (n == )
		return (1);
	return (n * factorial(n - 1));
}
