#include <stdio.h>

/**
 * main - print the alphabets in reverse
 * Return: Always 0
*/

int main(void)
{
	char le;

	le = 'z';
	while
		(le >= 'a') {
			putchar(le);
			le--;
		}
		putchar('\n');

	return (0);
}
